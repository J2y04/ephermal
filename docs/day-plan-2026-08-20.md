# Day plan, 20 August 2026

Written at 01:00 on the 20th, for when you wake up on the 20th. Ordered by what unblocks the most, not by what is most fun.

**Ground state:** last night's hardening pass is fully shipped and verified. Two financial-exposure holes closed in production (`21913ba`, `895721a`), risk assessment rewritten (`2803719`), both edge functions deployed and booting clean. Nothing from last night is left hanging. Start fresh.

---

## 1. The critical path, do this first (est. most of the value of the day)

Everything that leads to revenue is stuck behind one human conversation:

```
risk assessment  ->  Steuerberater  ->  business registration  ->  Stripe live  ->  7-day trial (#147)
      DONE              <- you are here
```

**Action: book or attend the Steuerberater meeting.** Take `docs/security-risk-assessment-2026-08-17.md` with you. It now answers what they asked for: it identifies the liability categories in order of realism, gives the real technical ceiling on worst-case automated spend, states the true current scale, and is explicit about which questions are theirs rather than mine.

Two things to raise with them specifically:
- The *Ermitteln meiner Haftung* question. The document's section "Where liability could actually arise" is written to be read directly by them.
- Ask whether the spend ceiling should be **contractual as well as technical**. A limitation-of-liability clause in the Terms addresses the same risk from the other side, and unlike code it does not depend on the next audit catching everything.

Nothing else on this list moves the business as far as this does. Resist the urge to open the editor first.

---

## 2. Verify two blockers I would not assert at 1am

Both come from older notes and were **not** re-checked last night. Confirm before trusting either. They gate real test users, which gates feedback, which gates the Meta ads spend plan.

- **Meta app testers.** Notes say the Meta app had **zero** testers registered, which would mean no real test user can complete OAuth regardless of anything in our code. Check the Meta developer console directly.
- **Google verification.** Notes say it was never actually submitted, correcting an earlier belief that it had been. Check the Google Cloud console directly.

If either is genuinely open, submitting them is cheap and the review clock starts running in the background while you do other work. Do it early in the day for that reason.

---

## 3. Your decisions, nobody else can make them

- **#141 Trademark.** A UK collision on "Ephermal" was found. I can research further, but proceed-or-rename is yours. This gets more expensive to change the longer it waits, so it deserves a decision rather than another deferral.
- **#143 Higgsfield model slug + Stripe UGC top-up price IDs.** Still unconfirmed, so the UGC video path cannot be verified end to end. Blocks nothing critical today, but it is a known hole in a shipped feature.
- **Migrations 001-016** have no entry in the ledger. Schema is live and correct, this is bookkeeping only. I deliberately did not fabricate history. Decide whether to reconstruct or to leave a documented note and move on. Leaving it is defensible.

---

## 4. Standing engineering checks (the recurring part you asked for)

You said you do not want to work without a plan. This section is meant to repeat, not to be done once.

**Every session, cheap:**
- `npm run build` before any push. Non negotiable, it has caught two failed production deploys already.
- Check Vercel deployment state after pushing. A green push is not a green deploy.
- Check Sentry for anything new overnight.

**Weekly, or after any feature touching money or auth:**
- Targeted security scan over the diff since the last audit, not a broad sweep. Last night's five-agent pass over one diff found more than the earlier hundred-agent sweeps did.
- Re-verify claims before repeating them. Last night's most serious finding, the unclamped budget scale, was found only because a "$10,000/day ceiling" claim was re-checked instead of restated. Assume old conclusions have rotted.
- Query the **live** database for RLS and grants rather than reading migration files. Those two have already been shown to drift.

**Known and deliberately unfixed, revisit when there is real traffic:**
- The standing-approval fast path skips the atomic claim used by `approve_action`, so two genuinely simultaneous duplicate requests could both execute. Needs a real idempotency key. Documented rather than half-patched, and low priority at one user.

---

## 5. If there is time left

Only after the above. These are real but none of them are blocking.

- Accessibility pass on the rebuilt admin panel. The marketing and dashboard surfaces had one, the new shadcn admin panel never did.
- The dashboard-polish work from Video 11, if you still want it.
- Fold `todo's.md` into something structured, since it is currently an untracked scratch file in the repo root.

---

## Do not re-derive these

Time savers, all verified on 19-20 August:

- Live scale: **1** `user_plans` row, **1** `user_integrations` row, 18 launched campaigns, **0** `ai_pending_actions` ever created.
- `ai-assistant` and `meta-api` run `verify_jwt: **true**`. The old note claiming all 26 functions were moved off the gateway is wrong as a blanket statement. Check per function.
- Deploy edge functions with the **global** `supabase` CLI, where you are logged in. `npx supabase` has no token.
- Clerk anti-abuse is fully configured. Bot protection, lockout, device trust, enumeration protection, subaddress blocking, disposable-email blocking. All six on. Do not revisit.
- Vercel production branch is `master`, not `main`.
